# Agent instructions

作業前に `docs/PRODUCT.md`、`docs/ARCHITECTURE.md`、`docs/DEVELOPMENT.md` と、変更対象に関係する最新コード・CIを読む。ユーザーの新しい指示を優先し、合意変更は文書にも反映する。

## 守る製品方針

- 名前は **meeshogi**。ミーアキャットをマスコットにする。
- iOS・Androidを同時に開発する。片方だけの実装を両OSで完了と報告しない。
- 無料の端末内解析・棋譜管理・戦績管理が製品の土台。LLM解説・助言は将来の有料機能。
- 初期版は端末内保存とKIF書き出し。ログイン、クラウド同期、LLM、課金を初期版の利用条件にしない。
- 将棋ウォーズ・棋桜のクリップボード棋譜を対象にする。サービスへのログインやスクレイピングを追加しない。
- 自由な分岐検討は可能にするが、分岐の永続保存は初期版の対象外。本譜・戦績を上書きしない。
- 1手詰め・3手詰めバッジは証明済みの結果だけを表示する。高い評価値やLLMの推測を詰み判定に使わない。
- 戦型分類は代表的な戦法と大分類に絞る。未分類を許容し、手動修正と自分／相手を分けた集計を用意する。

Issue #17 の移行期間はクラウド解析を staging で評価する。固定SFEN gate と benchmark、iOS・Android の受入が終わるまで端末内解析を保持し、production 環境は作らない。秘密情報と非公開の engine / weight は Git、公開 artifact、アプリ bundle に含めない。

## 実装と検証

- 本リポジトリの役割はアプリとモバイル統合。端末性能やネイティブ組み込みの条件を `sekirei-weight` のweight品質目標へ戻さない。
- 開発候補はReact Native / Expoと共有のネイティブ解析経路。未決の依存ライブラリ、バインディング、SDKバージョンを確定済みと扱わない。
- 依存を採用する際は現行の一次資料と利用条件を確認する。調査例として言及した他の将棋ソフトを採用済みと扱わない。GPL系コード・モデルの組み込みを前提にしない。
- 必要な境界だけを作る。将来のLLM、複数エンジン、同期のための汎用フレームワークを先行実装しない。
- パース、勝敗、合法手、持駒、成り、詰み証明、保存のような正しさが必要な箇所を重点的に検証する。文書だけの変更にアプリのテストを追加しない。
- ユーザーの対局者名をアプリの初期値に埋め込まない。サンプルの期待値としてのみ使用する。
- モバイル変更では両OSのビルド・起動と対象操作を検証する。画面の成功を報告する前に両OSのスクリーンショットを実際に確認する。
- CI通過、目視確認、実機確認を区別して報告する。未実行や失敗を成功として扱わない。

成果は変更内容・検証結果・残る制約が分かるPRにまとめる。READMEの状態と開発マイルストーンを実装実態に合わせる。
